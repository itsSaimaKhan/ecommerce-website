


// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   images: {
//     domains: ['cdn.sanity.io'],
//   },
//   reactStrictMode: true,
//   swcMinify: true,
// };

// module.exports = nextConfig; // Use `module.exports` only


/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.sanity.io'],
  },
  reactStrictMode: true,
  swcMinify: true,
};

export default nextConfig; // Use `export default`
